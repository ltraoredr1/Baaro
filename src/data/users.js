// Liste des utilisateurs réels (chargée depuis Supabase)
// Ce fichier sert uniquement de fallback en cas d'erreur de chargement

export const STABLE_USERS = [];

export function getUserById(id) {
  return null;
}
